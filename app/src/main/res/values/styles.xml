<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/white</item><!-- title_normal -->
        <item name="colorPrimaryDark">@color/gray</item><!-- 状态栏 -->
        <item name="android:textColorPrimary">@color/black</item><!-- 字体颜色 -->
        <item name="android:windowBackground">@color/white</item><!-- 背景颜色 -->
        <item name="colorAccent">@color/lighter_gray</item>
    </style>
    <!-- 横向线条 -->
    <style name="h_line">
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">2dp</item>
        <item name="android:background">@color/item_bg</item>
    </style>

    <!--自定义Alertdialog样式 -->
    <style name="custom_alertDialog" parent="Base.Theme.AppCompat.Light.Dialog">
        <item name="android:windowFrame">@null</item>  //Dialog的windowFrame框为无
        <item name="android:windowIsFloating">true</item>  //是否浮现在activity之上
        <item name="android:windowIsTranslucent">true</item> //是否半透明
        <item name="android:windowNoTitle">true</item>  //是否显示title
        <item name="android:background">@android:color/white</item>  //设置dialog的背景
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimAmount">0.7</item>  //就是用来控制灰度的值，当为1时，界面除了我们的dialog内容是高亮显示的，dialog以外的区域是黑色的，完全看不到其他内容
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:titleTextColor">@color/colorMainBlue</item>
    </style>
</resources>
